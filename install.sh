# Do not worry this doesnt break anything.
# Its only there because pip is weird on arch based systems.
# Also I tested it myself and it didnt break anything.
pip install -r requirements.txt --break-system-packages